#include <stdio.h>

/**
 * mul - Function that multiply two multiplicand a dnd b
 *@a:first multiplicand
 *@b:second multiplicand
 * Return:return c.
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
